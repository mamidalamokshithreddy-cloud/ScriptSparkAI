# ScriptSpark Frontend

React/Vite client for ScriptSpark.

```powershell
npm install
Copy-Item .env.example .env
npm run dev
```

Set `VITE_API_BASE_URL` to your backend API URL, including `/api`, when deployed.
